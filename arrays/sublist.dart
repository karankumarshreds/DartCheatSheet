/* sublist */
// Returns a new list containing the elements between start and end

void main() {
  var myList = ['a', 'b', 'c', 'd'];
  var newList = myList.sublist(0,2); 
  print(newList); // returns [a, b]
}
